name: Run Unit Tests

on: [push, pull_request]

jobs:
  tests:
    name: Test

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-12, windows-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - uses: 'actions/checkout@v3'
      - run: dotnet test
